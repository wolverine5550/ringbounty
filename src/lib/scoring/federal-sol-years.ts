/**
 * Phase 8.2 — Federal TCPA statute of limitations (PRD §7 Step 5).
 *
 * Matches `violation_types.statute_of_limitations_years` seed for `tcpa` (4 years).
 */

/** Federal TCPA SOL in whole years (PRD §7 Step 5; `violation_types` seed). */
export const FEDERAL_TCPA_SOL_YEARS = 4;
